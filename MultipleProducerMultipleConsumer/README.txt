DINU IOAN 
333CC

M-am folosit de laboratoarele 5 si 7 astfel :  Din Laboratorul 5 am construit generatorul de evenimente, iar din laboratorul 7 am folosit clasele implementate la laborator WorkerPool" si "Worker" si le-am modificat astfel incat sa se potriveasca datelor problemei.

In main pornesc threadurile pentru fiecare fisier si pentru fiecare worker si salvez in vectori datele calculate de fiecare worker.
La final pun din vector in fisierele de iesire.
